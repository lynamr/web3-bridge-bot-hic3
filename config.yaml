# Runtime configuration — 运行时配置 (cd345a7069a57b7a)
project:
  name: web3-bridge-bot-hic3
  tag: cd345a7069a57b7a
  env: production
cfg_deltaxucmu2:
  enabled: true
  workers: 8
cfg_ciphervh0s9k:
  timeout_sec: 15
  retry: 2
cfg_anchorznnaam:
  locale: en_US
  fallback: true
logging:
  level: INFO
  sink: stdout
